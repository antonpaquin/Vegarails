<div class="vc">
  <% @services[:self].each do |service| %>
    <div class="imgbox"><%= image_tag('/home/' + service + '.png') %></div>
  <% end %>
</div>
<div class="vc" style="margin-top: 50px;">
  <% @services[:third].each do |service| %>
    <div class="imgbox"><%= image_tag('/home/' + service + '.png') %></div>
  <% end %>
</div>
